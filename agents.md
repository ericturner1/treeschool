# AI Agent Instructions

1. Prevent regressions by verifying existing functionality before any commit.
2. Do not perform unasked-for work, refactors, or style-only changes unless explicitly requested.
3. Maintain strict adherence to the `ts-` naming convention for all services and packages.
4. Always prioritize the `FREE` requirement so the stack remains compatible with Vercel and Supabase free tiers.
